const taxa = 7.00;

const compra = Number(prompt("Informe o valor da compra"));

const valorTotal = compra + taxa;

console.log(`Valor de compra: R$${compra}\nValor total a pagar com taxa de R$${taxa}: R$${valorTotal}`)